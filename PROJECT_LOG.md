# PROJECT_LOG.md

## Current status · 2026-09-02

### 修复 · 提示计数在后续调度后丢失

* **根因**：提示 Worker 回调与二次提示分支原先直接执行
  `state = { ...state, hintsUsed: state.hintsUsed + 1 }`，改动的只是页面的镜像变量，
  没有同步到 `game.mjs` 控制器。下一次 `dispatch()` 从 `game.state` 取状态，
  会把这次累加覆盖掉，提示使用次数始终无法累积。
* **修复**：`createGame` 新增 `useHint()`，由控制器累加 `hintsUsed` 并返回内部状态；
  页面两处改为 `state = game.useHint()`，镜像与权威始终同一引用。
* **附带收益**：这两处原本违反 AGENTS.md 的“页面不得直接修改游戏状态”约束，
  现已收回控制器内，且累加逻辑首次可被 `game.test.mjs` 覆盖。

### 追加 · 验证

* `npm run test:orbit-sort`：51 项通过（新增 1 项提示计数回归），0 失败。
* `node --check` 验证 `game.mjs` 与 `main.mjs` 通过；`npm run build` 通过。

### 修复 · 页面层重复 stuck 判定与关卡质量回归

* 删除 `main.mjs` 中两处重复的 `stuck` 重算/覆盖逻辑；过期状态校正统一由 `game.mjs` 的
  `reconcile` 负责，页面只展示引擎结果。合法动作仍不会因死局被拦截。
* 为当前六关固定发布集增加首步死局数量回归：L2 必须记录 2 个，L4 必须记录 1 个；该指标
  用于发现题目质量退化，不把合法死局动作判为违规。

### 追加 · 验证

* `npm run test:orbit-sort`：51 项通过，0 失败；`git diff --check` 通过。

### 完成 · 清理死局遮罩遗留代码

* 删除已废弃的 `stuck-layer` DOM、样式和事件监听；死局现在只通过状态栏反馈，棋盘、撤销和重置始终可用。
* 保留引擎的 `stuck` 状态评估及“合法操作必须执行”语义，页面不再存在可重新阻塞操作的死局弹层入口。

### 追加 · 验证

* `npm run test:orbit-sort`：51 项通过，0 失败；`npm run build` 与 `git diff --check` 通过。

### 修复 · 补全剩余模块的版本占位符

* **缺口**：`main.mjs` 引用 `audio.mjs`、`daily.mjs`、`storage.mjs` 时没有 `?v=dev`，
  `daily.mjs` 引用 `../levels.mjs` 同样没有。构建只替换带占位符的引用，因此改动这三个
  客户端模块或关卡数据时，浏览器仍会执行缓存的旧代码——这是“修复后仍运行旧规则”的
  最后一条残留路径，且关卡数据经每日挑战加载时完全裸露。
* **修复**：四处引用统一补上 `?v=dev`，每日挑战路径加载的关卡数据也纳入版本替换。
* **回归保护**：新增标记测试遍历 `js/` 下所有客户端模块，断言每条相对 import 都以
  `?v=dev` 结尾；以后新增模块漏写版本会直接失败，不再依赖人工逐条列举。

### 修复 · 关卡生成器不再进入发布产物

* `generator.mjs` 只被测试引用、运行时不加载，但整目录拷贝会把它带进
  `dist/orbit-sort/`。现通过 `games.json` 的 `exclude` 排除，产物只含运行时文件。

### 追加 · 验证

* `npm run test:orbit-sort`：52 项通过，0 失败；`node --check` 验证 `main.mjs` 与
  `daily.mjs` 通过。
* `BUILD_ID=verify-002 npm run build`：17 处引用全部替换为同一构建号（含新增四处），
  门户首页与 CSS 未被误改；`dist/orbit-sort/` 已不含 `generator.mjs`。
* `git diff --check`：通过。

### 本轮收尾快照 · orbit-sort 稳定版

#### 本次完成

* 将发布主线重新生成为 6 个全新固定关卡，使用 `stabilize-alpha` 至 `stabilize-zeta` 种子；
  当前只暴露第 1–6 关，旧第 7–30 关保留为源码素材，不进入运行路径。
* 完成总规则器、意图控制器、页面启动入口、死局 advisory 和关卡首步可通关性审计；合法操作
  必须执行，死局只在操作后提示，非法操作才拒绝，通关状态才终止。
* 新增 `test:orbit-sort` 根级测试命令，并接入部署前 CI；测试失败不会继续发布。
* 完成无头 Edge 真实页面验收：选关节点、轨道渲染、合法入口调入和 placing 状态均正常。

#### 修改文件

* 工程与文档：`AGENTS.md`、`PROJECT_LOG.md`、`package.json`、`.github/workflows/deploy.yml`。
* 门户与构建：`games.json`、`scripts/build-site.mjs`、`assets/covers/orbit-sort.svg`。
* orbit-sort 页面与规则：`games/orbit-sort/index.html`、`engine.mjs`、`levels.mjs`、`solver.mjs`、
  `generator.mjs`、`favicon.svg`、`css/style.css`。
* orbit-sort 客户端：`js/main.mjs`、`bootstrap.mjs`、`game.mjs`、`renderer.mjs`、`audio.mjs`、
  `daily.mjs`、`storage.mjs`、`solver-worker.mjs`。
* orbit-sort 测试：`tests/engine.test.mjs`、`solver.test.mjs`、`levels.test.mjs`、`daily.test.mjs`、
  `storage.test.mjs`、`generator.test.mjs`、`rules-property.test.mjs`、`game.test.mjs`、
  `markup.test.mjs`。

#### 关键实现方式

* `validateAction` / `applyAction` / `applyIntent` 是唯一动作入口；页面只提交轨道或中转槽意图。
* `game.mjs` 封装状态、撤销、重置和存档状态恢复；`bootstrap.mjs` 负责显式启动页面。
* 求解器 Worker 区分提示与可通关性 advisory；只有完整搜索确认 `exhausted` 才提示无解，超时不误报。
* `validateLevel` 检查结构、颜色平衡、可解性及所有合法首步，并输出 `deadEndFirstMoves` 指标；
  该指标默认用于审计，不会阻止玩家执行合法动作。
* 构建统一改写资源版本号；源码使用 `v=dev`，生产构建使用单一构建号，覆盖 Worker 及内部 import。

#### 遇到的问题与处理

* 历史实现曾将引擎、页面和渲染器的判断分散，造成合法动作被拒绝、异色动作放行和旧 `stuck` 状态
  误报；现已集中到总规则器，并由 50 项测试和属性测试保护。
* 曾把死局提示实现为遮罩弹层，视觉上阻断了玩家；现改为非阻塞状态栏，保留撤销和重置。
* 新生成的高复杂度五色双槽候选求解成本过高，不纳入当前稳定版；第 6 关采用已完整验证的四色双槽
  基础规则布局，避免将求解不确定性带入发布。
* 历史日志保留用于审计；本快照作为下次开发的唯一当前基线，过期内容不应覆盖本节结论。

#### 最终验证

* `npm run test:orbit-sort`：50 项通过，0 失败。
* `npm run build`：通过。
* `git diff --check`：通过。

### 完成 · 可测试的游戏控制器

* 新增 `games/orbit-sort/js/game.mjs`，将页面交互收敛为 `createGame().dispatch(intent)`、`undo`、
  `reset` 和 `setState`；页面不再自行拼装动作或直接清除选择状态，统一由规则引擎解释轨道/中转槽意图。
* 新增控制器回归测试，覆盖双中转槽连续调入、真实死局保留以及过期 `stuck` 标记恢复；同步修正标记测试，
  明确 `applyIntent` 属于控制器/引擎层而非页面层。

### 追加 · 验证

* orbit-sort 全量测试 52 项通过；主页面与控制器语法检查通过。

### 完成 · 页面启动入口解耦

* `main.mjs` 现导出 `bootstrap()`，DOM 查询、Worker 创建和事件绑定只在显式启动时执行；新增
  `js/bootstrap.mjs` 作为页面唯一启动入口，降低页面联动测试对模块顶层副作用的依赖。
* 页面玩法、资源版本参数和既有控制器接口保持不变，仅替换 HTML 的模块入口并增加结构回归断言。

### 追加 · 验证

* orbit-sort 全量测试 52 项通过；`main.mjs` 与 `bootstrap.mjs` 语法检查通过，`git diff --check` 通过。

### 追加 · 真实页面交互验收

* 使用本地服务和无头 Edge 实际加载页面，点击第 1 关后确认生成 3 条轨道、2 个合法入口；
  点击合法入口后确认页面进入 `placing` 阶段、中转槽占用数为 1，状态提示同步切换为落轨提示。
* 本次未引入 DOM 测试依赖；规则正确性仍由引擎、控制器和属性测试覆盖，浏览器验收用于确认真实入口与渲染链路。

### 完成 · 合法死局的可通关性提示

* 复用提示 Worker 增加 advisory 请求：每次合法调度后异步运行更完整的求解预算；只有求解器返回
  `exhausted`、确认当前局面不存在通关路径时，才提示“建议撤销最近一步”。
* `timeout` 不被当作无解；撤销、重置、切关或下一次调度会使旧 advisory 请求失效。该层只提供信息，
  不锁定棋盘、不拦截合法操作，也不要求玩家遵循唯一解。

### 追加 · 验证

* Worker、主页面、规则与求解器定向测试通过；`main.mjs` 与 `solver-worker.mjs` 语法检查通过，
  `git diff --check` 通过。

### 完成 · 关卡首步可通关性质量门禁

* `validateLevel` 现在逐一检查所有合法首步，并记录 `deadEndFirstMoves` 与每个分支的状态；
  合法但会进入无解的分支不会被当作违规操作，也不会让本身存在通关路径的关卡失效。
* 支持可选的 `maxDeadEndFirstMoves` 发布门槛：超过门槛时拒绝关卡；求解超时标记为未知，
  不误报为无解。未来固定题目和随机题目可共用同一套质量指标。

### 追加 · 验证

* 关卡生成器测试 3 项通过；第 2 关回归确认 2 个合法死局首步可被记录，设置零容忍门槛时会被拒绝。
* `node --check games/orbit-sort/generator.mjs` 与 `git diff --check` 通过。

### 完成 · orbit-sort 持续集成门禁

* 根 `package.json` 新增 `test:orbit-sort`，集中执行 orbit-sort 的引擎、求解器、关卡、生成器、
  属性、控制器和标记测试；部署工作流在构建前执行该命令，测试失败时不会发布站点。

### 追加 · 验证

* `npm run test:orbit-sort`：53 项通过，0 失败。
* `npm run build` 与 `git diff --check`：通过；站点构建产物正常生成。

### 完成 · 稳定版收缩为五关

* 主线发布集现只暴露第 1–5 关，统一为第一章的五个递进难度；第 6–30 关的已验证数据暂留在
  源码目录作为后续扩展素材，不进入当前选关、每日挑战或发布运行路径。
* 选关章节、下一关按钮和相关测试改为基于当前发布集动态判断；前五关暂不启用冻结/单向等特殊机制，
  先集中验证基础规则和中转槽交互稳定性。

### 追加 · 验证

* `npm run test:orbit-sort`：50 项通过，0 失败。
* `npm run build` 与 `git diff --check`：通过；构建入口使用最新 `bootstrap.mjs`，发布集为五关。

### 完成 · 死局提示改为非阻塞反馈

* 合法操作执行后即使进入 `stuck`，页面也不再打开遮挡棋盘的死局弹层；状态栏明确提示刚才移动符合规则，
  玩家仍可查看盘面、撤销或重置。
* `stuck` 仍由总引擎正常评估，非法动作仍由统一规则器拒绝；本次只调整反馈层，避免把死局误解为违规。

### 追加 · 验证

* 5 关稳定版全量测试 50 项通过；`npm run build` 与 `git diff --check` 通过。

### 追加 · 五关运行时范围验收

* 使用本地服务和无头 Edge 实际加载选关页，确认仅渲染 5 个主线节点；第 1 关可用，第 2–5 关按
  解锁进度禁用，未出现第 6 关或旧的 30 关节点；每日入口仍正常显示。

### 完成 · 全新六关稳定版

* 使用新种子 `stabilize-alpha` 至 `stabilize-zeta` 重新生成第 1–6 关，替换原稳定版布局；
  前三关为单中转槽基础调度，后三关增加第四种颜色或双中转槽，难度逐步提高。
* 六关均通过统一规则器、求解器、首步可通关性检查和非预解校验；暂不启用冻结轨道、单向轨道等特殊机制。
* 发布集切换为 6 关，旧的第 7–30 关数据仍只作为源码中的后续素材，不进入当前运行路径。

### 追加 · 验证

* `npm run test:orbit-sort`：50 项通过，0 失败。
* `npm run build` 与 `git diff --check`：通过。

### 完成 · 页面意图统一交给总引擎

* 新增 `applyIntent`，页面只提交轨道/中转槽点击意图，由引擎根据真实槽位状态决定取出、落轨、
  选择槽或切换取出模式；页面不再自行拼装 extract/insert 动作，也不再直接调用清空选择状态。
* 新增 `clear-selection` 引擎动作，覆盖双槽连续调入、空槽切换、撤销和状态残留场景；保留
  “合法动作必须执行、死局只提示不拦截”的规则语义。

### 完成 · 固定 SVG 图层顺序

* 将对局 SVG 图层声明固定为 `background → tracks → track-effects → core → orbs → overlay-effects`，
  让星核/中转槽结构天然位于星球下方。
* 删除渲染器中的运行时 `insertBefore` 调整逻辑，新增结构顺序回归断言；规则、布局和交互不变。

### 完成 · 全局构建号资源版本统一

* orbit-sort 源码中的模块、CSS、Worker URL 统一使用本地 `v=dev`；生产构建脚本自动替换为
  单一 `BUILD_ID`（无环境变量时 CI 使用 GitHub SHA 或构建时间），不再手写多组规则/渲染版本号。
* 覆盖主入口、动态 Worker URL、Worker 内部 import、求解器、渲染器、生成器及 CSS；本地开发
  继续可直接运行，构建产物统一使用同一个构建号。

### 追加 · 总规则器可达状态属性审计

* 增加跨 30 关的可达状态属性测试，每关采样最多 5,000 个状态，验证合法动作必能执行、
  非法判定由统一入口负责、合法动作必产生状态变化，并检查 `stuck` 不会掩盖合法动作。
* 未采用无上限全状态遍历：第 6 关起状态空间达到 8 万级，测试设置明确上限并报告覆盖范围，
  避免验证任务本身失控；每关仍保留完整求解路径验证。

### 修复 · 多中转槽星球被星核遮挡

* 规则器本身支持多个中转槽；问题来自 SVG 图层顺序：星核图层覆盖了中转槽中的星球，视觉上
  像只能放入一个。现将 `core` 放到 `orbs` 之前，两个中转槽中的行星均可见，交互规则不变。
* 主页面与渲染器缓存版本升级为 `rules-v6` / `render-v3`。

### 修复 · 截图状态误弹死局提示

* 复核截图对应盘面：右侧绿色已进入空轨并与左侧绿色共存，规则器仍返回 4 个合法取出动作，
  不属于死局。页面现在每次渲染前会重新用总规则器校正 `stuck` 状态；若存在合法动作，自动
  恢复 `playing` 并关闭过期弹窗。
* 主页面缓存版本升级为 `20260901-rules-v5`；新增页面标记回归断言。

### 修复 · 星体消失的渲染运行时错误

* 移除星球符号后，渲染器仍残留 `SYMBOLS` 无障碍文本引用，导致浏览器在首次渲染时抛出
  `ReferenceError`，后续星体节点未能创建。现改为无符号的“颜色 N 的星球”描述并移除残留引用。
* 主页面与渲染器缓存版本升级，避免浏览器继续执行旧渲染模块。

### 追加 · 参考星云背景模拟

* 参考用户提供的深空图，将对局背景调整为黑蓝深空、蓝紫星云带、高密度星点与少量亮星的层次；
  星云由 SVG 模糊光带和 CSS 多层渐变生成，轨道增加航道外辉光，保持可缩放且不依赖位图。
* 增加减少动态效果下的静态降级，样式缓存版本升级为 `20260901-space-v2`。

### 追加 · 深空轨道环境渲染

* 在现有 SVG 基础上增加深空盘面、低透明度星云、远近星尘、轨道外层辉光与轨道内壁层次，
  让轨道更接近发光航道而不是平面线条；星云做低速漂移，减少静态背景感。
* 不引入位图背景，保留可缩放、可缓存和移动端适配；样式缓存版本升级为
  `20260901-space-v1`。规则、布局和交互不变。

### 追加 · 星体视觉升级

* 星体改为无符号的微型行星：使用颜色渐变、明暗面、大气边缘、云带、表面高光和柔和光晕，
  保留选中与完成描边状态；加入轻微漂浮和云带移动动画。
* `prefers-reduced-motion` 下关闭行星动画；样式缓存版本升级为
  `20260901-planets-v1`，确保浏览器加载新视觉资源。

### 追加 · 关卡生成与发布前验证

* 新增 `games/orbit-sort/generator.mjs`：固定种子生成颜色分布，使用总规则器构造状态，
  再用求解器确认至少存在通关路径并计算 PAR；同一种子重复生成结果一致。
* `validateLevel` 统一检查容量、中转槽、颜色数量、轨道容量、特殊轨道、初始未完成和可解性，
  固定关卡与未来随机题共用同一验证入口；生成器不会直接把未经验证的随机题交给页面。
* 新增生成器回归测试；本轮不改变现有 30 关和每日挑战数据。

### 追加 · 统一底层规则器接口

* 新增 `validateAction`、`applyAction`、`evaluateState`，集中处理取出、落轨、中转槽选择、
  非法原因和局面状态；页面点击路径改为由 `applyAction` 返回结果。
* `stuck` 仍是局面评估结果，不是操作锁；合法动作不因是否会走入死局而被拒绝，只有通关状态
  禁止继续调度。新增统一接口回归测试，确保合法动作可执行、非法动作返回明确原因。

### 追加 · 合法死局与非法操作反馈分离

* **反馈修正**：合法移动后若局面确实无后续动作，不再显示“星核拥堵了”，改为“当前无后续调度”，
  并明确说明刚才的移动符合规则。
* **规则边界**：异色落轨、满轨和冻结等真正违规操作仍保持“无法落轨”反馈；撤销入口保留。
* **回归保护**：保留同色转移与异色拒绝测试，并增加状态/页面文案检查。

### 追加 · 验证

* 引擎、求解器与标记测试 32 项通过，主页面语法检查和 `git diff --check` 通过。

### 追加 · 规则错误重复出现的最终加固

* **强制版本切换**：主页面、引擎、渲染器、关卡、求解器和提示 Worker 统一切换到
  `20260901-rules-v2`，排除旧页面模块缓存继续运行的可能。
* **拥堵保护**：页面 transition 在显示拥堵弹窗前再次计算合法动作；只要存在合法动作，状态
  自动保持 `playing`，不会显示错误拥堵。
* **截图路径验证**：关卡 2 连续执行“右侧绿色 → 左侧、右侧绿色 → 左侧”后状态保持 playing，
  两次均产生正确同色落轨；红色入口落入绿色轨道仍保持拒绝。

### 追加 · 验证

* 引擎与标记测试 23 项通过，连续操作复现通过，主页面/渲染器语法检查和 `git diff --check` 通过。

### 追加 · 规则一致性加固：禁止错误拥堵状态

* **状态判定加固**：`isStuck` 现在以可玩状态重新计算合法动作，旧的 `stuck` 标记不能遮蔽
  仍然存在的合法调度。
* **回归场景**：新增截图对应的“绿色入口接收绿色”与“红色入口落入绿色”测试，分别确保合法
  操作继续进行、异色操作保持原状态。

### 追加 · 验证

* 引擎、标记测试共 23 项通过；主页面与渲染器语法检查、`git diff --check` 通过。

### 追加 · 响应式视觉验收

* **静态视口检查**：确认移动端 `max-width: 700px`、桌面端 `min-width: 900px` 两套规则均存在；
  移动端棋盘尺寸、两行 HUD 与 44px 触控按钮规则均已命中。
* **运行时资源检查**：本地 HTTP 返回最新 HTML/CSS，页面版本参数正确，未发现资源路径错误。
* **限制**：当前环境无可调用的 Edge/Chrome/Playwright 无头浏览器，本轮未生成真实截图；已明确记录，
  待具备浏览器环境后再做像素级验收。

### 追加 · 验证

* 标记测试、语法检查和 `git diff --check` 通过。

### 追加 · 对局页整体视觉重构第 7 轮：移动端控制舱

* **手机 HUD**：压缩标题区并将步数、目标、星图进度整理为第二行信息舱，避免顶部拥挤。
* **棋盘优先**：重新分配移动端纵向空间，扩大棋盘可用区域，状态提示保持独立且可读。
* **触控工具**：底部控制舱改为更大的触控按钮与自适应间距，保留键盘和桌面布局。

### 追加 · 验证

* 标记测试、渲染器语法检查和 `git diff --check` 通过。

### 追加 · 对局页整体视觉重构第 6 轮：能量动效

* **合法目标**：落轨阶段增加沿轨道流动的能量线，帮助玩家理解“星核 → 目标轨道”的方向。
* **星核运行**：核心轨道环持续低速旋转，选中星体的能量晕进行呼吸式强调。
* **动效边界**：所有新增动效在 `prefers-reduced-motion: reduce` 下关闭，不影响操作和信息读取。

### 追加 · 验证

* 对局引擎、标记测试、渲染器语法检查和 `git diff --check` 通过。

### 追加 · 对局页整体视觉重构第 5 轮：事件面板

* **结算面板**：通关、重置确认和卡住提示统一为深空事件面板，增加星核光环、玻璃层、
  事件色带和明确的操作按钮层级。
* **状态区分**：卡住使用红紫警示色，重置使用冷青确认色，通关保持琥珀能源色；不改变
  原有对话框焦点管理、撤销和结算逻辑。

### 追加 · 验证

* 对局标记测试、主页面语法检查和 `git diff --check` 通过。

### 追加 · 选关页整体视觉重构第 1 轮：星图导航

* **星域面板**：章节区域改为半透明星域舱，加入星环、星尘、章节色带与层次阴影。
* **关卡节点**：未解锁、当前、已完成节点分别使用不同的星体状态；可操作节点增加悬停和按下反馈，
  保留原有星级、解锁顺序和可访问标签。
* **入口按钮**：继续游戏与每日挑战改为深空能源舱风格，并与对局页使用同一套视觉变量。
* **范围**：仅调整选关页 CSS，不改选关逻辑、存档格式或关卡数据。

### 追加 · 验证

* 选关/对局标记测试通过，主页面语法检查与 `git diff --check` 通过。

### 追加 · 对局页整体视觉重构第 3 轮：星体能量单位

* **星体升级**：每颗星体增加独立能量晕、内嵌环和颜色纹章层，视觉上从普通彩色圆点升级为
  可识别的宇宙能量单位。
* **交互可读性**：选中星体的内环改用冷青高亮，继续保留 `+ / - / =` 等色觉辅助符号，
  不依赖颜色单独传达状态。
* **范围**：只调整星体 SVG 节点与现有 CSS 表现，不改变状态机和交互规则。

### 追加 · 验证

* 对局引擎与标记测试通过，渲染器语法检查和 `git diff --check` 通过。

### 追加 · 对局页整体视觉重构第 2 轮：装置化棋盘

* **轨道结构**：增加入口舱、远端端点和能量刻度，让轨道具备可读的空间结构与流向。
* **星核结构**：增加外层轨道环与中心信标，强化中转槽所在的核心装置层级。
* **范围**：只调整现有 SVG 棋盘图层与 CSS 表现，不改变规则、状态机或操作方式。

### 追加 · 验证

* 对局引擎与标记测试通过，渲染器语法检查和 `git diff --check` 通过。

### 追加 · 对局页整体视觉重构第 1 轮

* **视觉系统**：统一深空黑、蓝紫星云、冷青能量与琥珀进度色，补充半透明控制台层、内阴影和
  发光层次，减少原有平面化的深蓝面板感。
* **对局 HUD**：步数、PAR、星图进度改为独立信息舱；返回、声音和工具按钮增加悬停、按下与
  深空玻璃质感。
* **棋盘氛围**：棋盘外围增加引力光环，轨道与星核强化微光和中心聚焦，底部工具区明确为控制舱。
* **范围**：本轮只改对局页 CSS，不改关卡、规则、选关页和资源依赖。

### 追加 · 验证

* 对局引擎、标记测试与渲染器语法检查通过，`git diff --check` 通过。

### 追加 · 宇宙氛围 UI 优化

* **深空背景**：增加深空渐变、蓝紫星云光晕、星尘点与面板层次，降低原有纯深蓝面板感。
* **棋盘质感**：固定星尘绘制在星轨背景上，轨道增加微弱辉光，星核强化边缘光与视觉焦点。
* **范围**：仅调整现有 CSS 与 SVG 渲染表现，不改变游戏规则、关卡、交互结构或资源依赖。

### 追加 · 验证

* 引擎、关卡、求解器、存档、每日与标记测试通过；渲染器语法检查与 `git diff --check` 通过。

### 追加 · 规则一致性修复：轨道星体视觉位置

* **根因**：引擎按 `[远端, ..., 入口]` 保存轨道，但渲染器按固定容量从外向内绘制，导致轨道未满时
  星体视觉上没有贴合入口；玩家看到的可操作入口与引擎实际入口不一致。
* **修复**：渲染时按当前轨道长度从入口向外紧密排列，保持引擎入口、视觉入口和合法落轨判断一致。
* **回归保护**：新增渲染位置断言，防止恢复固定容量坐标算法。

### 追加 · 验证

* 引擎与标记测试通过，主页面、渲染器和提示 Worker 语法检查通过，`git diff --check` 通过。

### 追加 · 规则冲突复现：同色转移不应触发拥堵

* **截图状态复核**：对应“右侧绿色入口调入星核，再落入左侧已有绿色轨道”的状态，当前引擎判定
  左侧为合法目标，转移后仍为 `playing`，不会触发 `stuck`。
* **回归保护**：新增截图对应状态测试，覆盖右侧绿色调入、左侧同色落轨和不触发拥堵三个条件；
  同时保留异色落轨必须拒绝的连续操作测试。

### 追加 · 验证

* 引擎与完整游戏测试通过；模块版本参数已更新，避免旧规则模块缓存继续运行。

### 追加 · 规则冲突修复：旧模块缓存与非法连续操作

* **确认问题**：第 2 关“红色落入空轨后，蓝色归入蓝轨，再将绿色落入红轨”的截图序列中，
  第三步必须被拒绝；引擎实测保持原状态，不允许异色入口落轨。
* **页面修复**：主页面、渲染器、求解器与提示 Worker 的规则模块加入版本查询参数，避免浏览器
  继续复用旧版模块；新增该连续操作回归测试，覆盖合法前两步与非法第三步。

### 追加 · 验证

* 完整游戏测试 41 项通过，语法检查与 `git diff --check` 通过。

### 追加 · 规则审计第 7 轮：第三章关卡复核

* **复核范围**：第 21–30 关逐关使用 2,000,000 节点 / 5 秒预算验证；单向轨道、冻结轨道与
  六色布局均可由求解器复放通关。
* **本轮落地**：第 24 关替换为非循环混排布局，最优首步由 5 个收敛为 2 个，PAR 由 12
  重算为 16。其余关卡暂保留原布局，避免在未获得稳定完整预算结果前强行改动。

### 追加 · 验证

* 第三章关卡测试通过；全量游戏测试继续通过，语法检查与 `git diff --check` 通过。

### 追加 · 规则审计第 6 轮：第 13–20 关策略收敛

* **章节二中段重做**：第 13–20 关替换为五色、容量 4、双中转槽的混排布局；第 16、18、20
  关继续保留冻结轨道，并让冻结轨道在求解路径中延后解锁，确保机制真实影响调度。
* **公平性校准**：在 2,000,000 节点 / 5 秒完整预算下重新计算 PAR，分别为 16、13、19、17、
  13、16、14、16；最优首步分支均控制在 1–3 个，允许等价合法解但避免高度对称的开局。
* **回归保护**：新增第 13–20 关首步分支审计，防止后续布局再次退化为大量等价试探。

### 追加 · 验证

* 30 关求解与路径复放、完整游戏测试 40 项、语法检查与 `git diff --check` 均通过。

### 追加 · 规则审计第 5 轮：第 11–12 关分支预算校准

* **章节二前段重做**：第 11、12 关保持五色、容量 4、双中转槽，替换为更具策略性的
  混排布局，PAR 分别为 16、15。
* **统一完整预算复核**：在 2,000,000 节点 / 5 秒预算下，第 11 关有 3 个、第 12 关有
  2 个最优首步；此前较短候选搜索遗漏了第 11 关的一个分支，已将回归门槛修正为最多 3 个。
  这保留了公平的等价解，同时避免错误声称唯一。

### 追加 · 验证

* 30 关求解与复放继续全部通过；完整游戏测试 39 项通过，语法与差异检查已通过。

### 追加 · 规则审计第 4 轮：第 6–10 关分支收敛

* **中段早期关卡重做**：第 6–9 关保持四颜色、容量 3、双中转槽，第 10 关保持四颜色、
  容量 4、双槽，仅替换混排布局并重新计算 PAR。
* **开局策略收敛**：第 6–10 关最优首步分支数分别为 2、1、2、1、2，相比原布局的
  3–4 个分支更容易形成明确的策略方向，同时仍允许等价合法解。
* **质量门槛**：新增分支数量回归，防止后续修改让早期双槽关重新退化为高度对称的试探题。

### 追加 · 验证

* 30 关求解与复放全部通过；全量游戏测试 38 项通过，`git diff --check` 通过。

### 追加 · 规则审计第 3 轮：第 3–5 关唯一策略骨架

* **重做早期布局**：第 3、4 关保留三色容量 3，第 5 关保留四色容量 3，仅替换混排布局；
  三关均通过 0-1 BFS，且最优首步分别唯一落在轨道 0、0、1。
* **修正一次元数据错配**：候选布局迁移时发现第 3 关曾错误保留旧 PAR=7，已按当前布局
  重算为 PAR=8；现在测试直接防止布局与 PAR/关键首步断言脱节。

### 追加 · 验证

* 30 关求解与复放全部通过；全量游戏测试 37 项通过，`git diff --check` 通过。

### 追加 · 规则审计第 2 轮：早期关卡唯一策略

* **第 2 关重做**：替换为无预解、三色三容量布局，PAR=6；最优首步审计确认仅有
  `trackId=1` 一条关键首步，玩家不会因为多个等价开局而误以为存在唯一标准答案。
* **质量门槛落地**：新增 `analyzeOptimalFirstActions` 的关卡回归，明确区分“规则可解”、
  “最优”与“最优首步唯一”；第 1 关仍保留两条等价教学开局，不把合法解误判为错误。

### 追加 · 验证

* 30 关求解与复放继续全部通过；全量游戏测试 36 项通过，语法与差异检查通过。

### 追加 · 规则审计：多解与可操作路径

* **确认规则现状**：通关判定接受任何满足引擎规则的完成状态，不要求复制提示 Worker 的
  某一条路径；`PAR` 只代表最少调度数。对当前 30 关逐关检查，全部可解且 PAR 与求解器
  一致，但第 1 关已有 2 个同样最优首步，其他关也存在多个最优首步。
* **修正双槽交互不一致**：引擎/求解器允许已有一颗星体时继续调入第二颗，但界面原先
  没有退出落轨选择的入口。现在点击空中转槽可切回调入模式，玩家能复现双槽求解路径。
* **新增质量工具**：`analyzeOptimalFirstActions` 暴露最优首步分支数，测试明确记录多解
  而非把某条提示路径误当唯一答案。后续重做关卡前可据此筛选“策略骨架”复杂度。

### 追加 · 验证

* 30 关求解与复放全部通过；新增规则审计测试后游戏测试共 34 项通过，语法与差异检查
  通过。尚未强行将关卡改为字面唯一解，以避免把合法且等价的策略错误判为失败。

### 追加 · 《星轨调度大师》体验优化第 6 轮：可视状态引导

* **新增可视状态条**：将原本仅通过 `aria-live` 播报的操作提示显示在棋盘下方；空槽时
  明确提示选择轨道入口，持有星体时明确提示选择高亮落点，非法、卡住和通关文案继续
  复用同一播报通道。
* **保持信息克制**：状态条只解释当前操作阶段，不显示求解答案；合法目标仍由棋盘高亮
  表达，玩家保留完整决策空间。

### 追加 · 验证

* `node --check games/orbit-sort/js/main.mjs`、完整游戏测试 33 项与 `git diff --check`：通过。

### 追加 · 《星轨调度大师》体验优化第 5 轮：传输因果反馈

* **新增能量传输轨迹**：调入时显示来源轨道到中央星核、落轨时显示星核到目标轨道的
  短暂能量线，和星体位移动画同步，强化“从哪里来、到哪里去”的操作因果感。
* **保持操作节奏**：轨迹仅存在约 300ms，不锁定棋盘、不阻塞撤销和连续操作；减少动态
  效果模式下自动压缩为瞬时反馈。

### 追加 · 验证

* `node --check` 验证 `main.mjs` 与 `renderer.mjs` 通过；完整游戏测试 33 项通过，
  `git diff --check` 通过。本轮未调用外部调研服务，未产生 429 风险。

### 追加 · 《星轨调度大师》体验优化第 4 轮：主线方向感

* **星图明确下一目标**：当前最高已解锁、尚未完成的关卡使用独立蓝色光标与金色能量点；
  完成一关后光标自然移至新解锁节点，已完成节点继续仅表达星级。
* **避免伪目标**：不引入货币、日常清单或强制任务，主线节点本身就是唯一且清晰的下一步。

### 追加 · 验证

* `node --check games/orbit-sort/js/main.mjs`、`node --test games/orbit-sort/tests/markup.test.mjs` 与 `git diff --check`：通过。

### 追加 · 《星轨调度大师》体验优化第 3 轮：落点可读性

* **明确持有态**：中转槽选中星体后，渲染器向棋盘暴露 `placing` 阶段；所有合法落轨
  目标使用更亮的描边、柔和光晕与短促呼吸，非法轨道保持低调。
* **不替代解谜**：反馈只显示当前规则允许的落点，不标示“正确”路径；玩家仍需决定
  保留空间、合并颜色或完成哪一条轨道。
* **动态效果适配**：减少动态效果模式下保留高对比静态落点状态，不播放循环呼吸。

### 追加 · 验证

* `node --check games/orbit-sort/js/renderer.mjs`、`node --test games/orbit-sort/tests/markup.test.mjs` 与 `git diff --check`：通过。

### 追加 · 《星轨调度大师》体验优化第 2 轮：首分钟难度曲线

* **首关改为真正教学局**：容量由 3 调整为 2，两色星体各两颗、三轨、单中转槽，PAR
  由 6 降为 3。玩家只需理解“调入、借空轨、落回同色”这一条核心回路即可完成。
* **修正早期数值倒退**：前四关的最优调度现为 3 → 4 → 6 → 7；第 2 关才引入第三种
  颜色，第 3 关开始要求更长的周转，不再出现首关比后续教学关更重的情况。
* **验证**：30 关仍全部非预解、求解器最优 PAR 与逐步复放通关；每日同构、存档与界面
  标记回归均通过。

### 追加 · 《星轨调度大师》体验优化第 1 轮：首局节奏与触感

* **竞品取舍**：参考公开 Nut Sort / Ball Sort 产品与玩家反馈，保留无时间压力、无限撤销
  和可验证关卡，避免广告、硬币和无用途的伪奖励；优先落实即时反馈、局部完成和渐进难度。
* **首局难度重排**：第 1 关改为两色、三轨、单中转槽，PAR 从 9 降至 6；第 2 关才引入
  第三种颜色。首关仍非预解且由 0-1 BFS 证明最优，光圈引导对应可立即开始的轨道。
* **棋盘反馈增强**：HUD 显示已充能轨道数；中转槽当前选中的星体拥有独立描边与光晕，
  星核、已完成轨道和背景层增加克制的能量层次，减少动态效果模式保持静态可读。
* **本轮边界**：未增加随机障碍、付费道具、广告或额外规则；中后段关卡数据保持不变。

### 追加 · 验证

* `node --test games/orbit-sort/tests/engine.test.mjs games/orbit-sort/tests/solver.test.mjs games/orbit-sort/tests/levels.test.mjs games/orbit-sort/tests/daily.test.mjs games/orbit-sort/tests/storage.test.mjs games/orbit-sort/tests/markup.test.mjs`：33 通过，0 失败。
* `node --check games/orbit-sort/js/main.mjs` 与 `git diff --check`：通过。未触及门户数据或
  构建脚本，本轮无需运行根构建。

### 完成 · 《星轨调度大师》V1 与门户接入

* **交互与无障碍收尾**：重置改为确认对话框；卡住和通关使用可访问弹层，打开后焦点进入
  对应操作，`Esc` 可关闭并回到适当入口。第 1 关首次进入显示轨道光圈，首次有效操作后
  自动消失；减少动态效果下改用静态描边。
* **长期进度反馈**：主线新完成节点以一次短暂星光反馈显示，结算显示“新纪录”；每日、
  主线、声音偏好可共存。读取中途局时额外校验轨道/中转槽结构、颜色范围、总星体数、
  唯一 id、选择槽位和最多 200 条历史，非法数据不进入对局。
* **门户发布条目**：新增真实玩法封面 `assets/covers/orbit-sort.svg`，登记
  `games.json` 的 `orbit-sort` 条目。构建后生成 `dist/orbit-sort/`，首页卡片、JSON-LD
  与 sitemap 均由既有数据流纳入该游戏。
* **范围结论**：V1 的 30 关、特殊机制、SVG 棋盘、响应式布局、存档、进度/星图、每日、
  提示、音效、色觉符号、键盘和门户接入均已完成。本轮未添加广告、账号、远程服务或
  其他未批准机制。

### 追加 · 最终验证

* `node --test games/orbit-sort/tests/engine.test.mjs games/orbit-sort/tests/solver.test.mjs games/orbit-sort/tests/levels.test.mjs games/orbit-sort/tests/daily.test.mjs games/orbit-sort/tests/storage.test.mjs games/orbit-sort/tests/markup.test.mjs`：33 通过，0 失败。
* `node --check` 验证修改的游戏模块通过；`npm run build` 通过。
* 确认 `dist/orbit-sort/index.html`、`dist/assets/covers/orbit-sort.svg` 与构建后的
  `dist/games.json` 均存在；Edge 1366×768 游戏选关页和 1200×900 门户首页截图均正常。

### 追加 · 《星轨调度大师》阶段 5f：每日星轨

* **新增离线每日挑战**：`js/daily.mjs` 以本地自然日为种子，从已验证的主线布局生成
  确定性的颜色置换版本。同日始终相同，不读取网络、不使用运行时随机，且保持源关的
  参数与最优 PAR。
* **新增每日进度与连胜**：存档保存当天未完成盘面、当日最佳步数、连续完成天数和历史
  最长连胜。当天重复通关只更新最佳步数，不重复增加连胜；仅连续本地日期才延续连胜。
* **新增入口与恢复**：选关页提供“今日星轨”，存在当天有效中途局时显示继续入口；
  每日结算不显示主线下一关。每日日期标识随调度、撤销与重置状态保留。
* **本轮边界**：每日关使用求解器已验证主线的颜色同构，不新增未经验证的随机布局；
  未实现星图连线动画、存档版本迁移或门户接入。

### 追加 · 验证

* `node --test games/orbit-sort/tests/engine.test.mjs games/orbit-sort/tests/solver.test.mjs games/orbit-sort/tests/levels.test.mjs games/orbit-sort/tests/daily.test.mjs games/orbit-sort/tests/storage.test.mjs games/orbit-sort/tests/markup.test.mjs`：32 通过，0 失败。
* `node --check` 验证 `engine.mjs`、`daily.mjs`、`main.mjs` 与 `storage.mjs` 通过；
  本地服务 `js/daily.mjs` 返回 HTTP 200，Edge 桌面截图确认入口布局。未触及门户、
  `games.json`、构建脚本或部署，无需运行根构建。

### 追加 · 《星轨调度大师》阶段 5e：三章星图

* **选关升级为章节星图**：30 个关卡按已有 `level.chapter` 分成三条十节点星轨，显示
  “初启航路 / 冰封星域 / 逆行天环”及各章完成数量；不改动关卡顺序、解锁规则或存档格式。
* **节点状态保持一致**：未解锁节点禁用，已完成节点显示最佳星级并点亮连线；每个节点有
  完整的中文可访问名称，包含关卡号与锁定、可开始或星级状态。
* **响应式星图**：桌面横向展示十节点路径，700px 以下改为四列折返路径，避免窄屏横向
  滚动或节点文字裁切。
* **本轮边界**：未实现星图完成连线动画、每日挑战、存档版本迁移或门户接入。

### 追加 · 验证

* `node --test games/orbit-sort/tests/engine.test.mjs games/orbit-sort/tests/solver.test.mjs games/orbit-sort/tests/levels.test.mjs games/orbit-sort/tests/storage.test.mjs games/orbit-sort/tests/markup.test.mjs`：27 通过，0 失败。
* `node --check games/orbit-sort/js/main.mjs` 通过；Edge 1200×900 截图确认三章路径、
  节点、锁定态和分隔正确。未触及门户、`games.json`、构建脚本或部署，无需运行根构建。

### 追加 · 《星轨调度大师》阶段 5d：声音偏好持久化

* **声音设置随进度保存**：`doin.orbit-sort.progress.v1` 新增兼容的
  `settings.soundOn`。关闭声音后立即写入，刷新后音频实例和按钮状态均按保存值初始化；
  旧版进度没有该字段时保持默认开启。
* **避免后续写入回退设置**：通关记录继续保留声音偏好，不会因解锁或最佳成绩写入而把
  静音状态重置。
* **新增存储回归测试**：验证旧版和空存档的默认值，以及静音经完成记录后仍保留。
* **本轮边界**：未实现每日挑战、章节星图、存档版本迁移或门户接入。

### 追加 · 验证

* `node --test games/orbit-sort/tests/engine.test.mjs games/orbit-sort/tests/solver.test.mjs games/orbit-sort/tests/levels.test.mjs games/orbit-sort/tests/storage.test.mjs games/orbit-sort/tests/markup.test.mjs`：27 通过，0 失败。
* `node --check` 验证 `main.mjs`、`storage.mjs` 与 `audio.mjs` 通过；未触及门户、
  `games.json`、构建脚本或部署，无需运行 `npm run build`。

### 追加 · 《星轨调度大师》阶段 5c：提示 Worker

* **新增离线推演提示**：`js/solver-worker.mjs` 在独立模块 Worker 中运行既有确定性
  0-1 BFS，主线程不做随机猜测；900ms 或节点预算内未得到路径时提示用户先撤销尝试。
* **新增两段提示交互**：正常状态下首次提示标出应调入的来源轨道，3 秒内再次提示标出
  对应落轨目标，并计入一次提示使用；若星体已在中转槽，则直接标出当前应落入的轨道。
* **提示状态可取消**：有效调度、撤销、重置、切关都会清除高亮并使未完成的 Worker 请求
  失效，避免旧盘面的结果覆盖新盘面；Worker 加载失败时恢复按钮并给出可用反馈。
* **移动端选关修正**：为关卡网格补足 `width:100%`，防止窄屏下自动列计算越出页面。
* **本轮边界**：未实现每日挑战、星图、存档版本迁移或门户接入。

### 追加 · 验证

* `node --test games/orbit-sort/tests/engine.test.mjs games/orbit-sort/tests/solver.test.mjs games/orbit-sort/tests/levels.test.mjs games/orbit-sort/tests/markup.test.mjs`：25 通过，0 失败。
* `node --check` 验证 `main.mjs`、`renderer.mjs` 与 `solver-worker.mjs` 通过；本地服务中
  `js/solver-worker.mjs` 返回 HTTP 200。
* Edge 无头截图检查了 1200×900 与 390×844 选关布局；未触及门户、`games.json`、构建
  脚本或部署，无需运行 `npm run build`。

### 追加 · 《星轨调度大师》阶段 5b：中途对局恢复

* **扩展本机存档**：`doin.orbit-sort.progress.v1` 现保存未完成对局的完整纯数据状态。每次
  有效调度、落轨、撤销和重置后写入；通关记录写入后清除未完成盘面。
* **刷新恢复**：启动时验证存档的关卡 id、容量、轨道数、中转槽数与对局状态；验证通过
  则直接恢复该盘面，验证失败则保留在选关页，避免坏数据令页面崩溃。
* **选关入口**：存在合法未完成盘面时显示“继续上次对局”；无存储权限时仍可正常游玩。
* **本轮边界**：尚未实现存档版本迁移、200 条历史裁剪、声音/辅助设置持久化、每日挑战、
  提示 Worker 或星图。

### 追加 · 验证

* `node --test games/orbit-sort/tests/engine.test.mjs games/orbit-sort/tests/solver.test.mjs games/orbit-sort/tests/levels.test.mjs games/orbit-sort/tests/markup.test.mjs`：24 通过，0 失败。
* `node --check games/orbit-sort/js/main.mjs` 与 `node --check games/orbit-sort/js/storage.mjs`：通过；未触及门户、`games.json`、构建脚本或部署，无需运行 `npm run build`。

### 追加 · 《星轨调度大师》阶段 5a：主线进度闭环

* **新增默认选关页**：首次打开显示 30 个主线节点，仅第 1 关初始可进入；对局页返回
  后回到选关，已完成节点显示星级。
* **新增通关结算与解锁**：通关后显示星级、调度数与 PAR，可重玩或进入下一关；首次
  通关解锁下一节点。
* **新增轻量本机进度**：`js/storage.mjs` 保存 `unlockedLevel` 与各关最佳星级/步数；
  localStorage 不可用时仍可游玩。本轮不保存中途盘面，完整恢复留后续存档任务。
* **修复结算层初始显示**：显式为 `[hidden]` 结算层设置 `display:none`，避免 CSS 的
  `display:grid` 覆盖原生隐藏属性。

### 追加 · 验证

* Edge 1200×900 截图确认默认选关页、首关可用状态与隐藏结算层正确。
* `node --test games/orbit-sort/tests/engine.test.mjs games/orbit-sort/tests/solver.test.mjs games/orbit-sort/tests/levels.test.mjs games/orbit-sort/tests/markup.test.mjs`：24 通过，0 失败。
* 未触及门户、`games.json`、构建脚本或部署，无需运行 `npm run build`。

### 追加 · 《星轨调度大师》修复：桌面棋盘空白

* **修复 ES module 相对路径**：`js/main.mjs` 与 `js/renderer.mjs` 原先从 `js/` 目录
  错误引用 `./engine.mjs` / `./levels.mjs`，浏览器请求不存在的 `/js/engine.mjs` 与
  `/js/levels.mjs`，导致入口模块在首次渲染前失败。现改为正确的根模块相对路径
  `../engine.mjs` 与 `../levels.mjs`。
* **补足 SVG 固有尺寸**：棋盘 SVG 声明 `width="1000" height="1000"`，桌面布局改为
  受可视区域限制的确定高度，避免 Grid 环境下 `height:auto` 使棋盘失去可见尺寸。
* **新增路径回归断言**：标记测试验证入口与渲染器均从 `js/` 正确引用根目录规则/关卡
  模块，防止资源 HTTP 200 但模块图实际 404 的回归。

### 追加 · 验证

* Edge 无头截图已确认 1200×1200 桌面下放射轨道、星核与彩色星体正常显示。
* `node --test games/orbit-sort/tests/engine.test.mjs games/orbit-sort/tests/solver.test.mjs games/orbit-sort/tests/levels.test.mjs games/orbit-sort/tests/markup.test.mjs`：24 通过，0 失败。
* `/games/orbit-sort/engine.mjs`、`levels.mjs`、`js/main.mjs` 与 `js/renderer.mjs` 均返回 HTTP 200；未触及门户、`games.json`、构建脚本或部署，无需运行 `npm run build`。

### 追加 · 《星轨调度大师》阶段 4：动效与反馈

* **新增状态驱动的 SVG 反馈**：星体沿既有 keyed SVG 节点在轨道与星核间短距移动；
  充能轨道触发一次能量环，冻结解除触发一次冰蓝环，非法轨道或中转槽短促闪烁。逻辑状态
  先更新后反馈，不用全局 `pointer-events: none` 锁住撤销或声音按钮。
* **新增零资源 WebAudio**：`games/orbit-sort/js/audio.mjs` 用合成音覆盖调入、落轨、
  非法、完成和解冻。`AudioContext` 仅在用户手势中初始化；顶栏声音按钮可即时开关，
  本轮尚未接入存档持久化。
* **减少动态效果适配**：`prefers-reduced-motion: reduce` 下星体移动压缩到 80ms，
  脉冲与抖动压缩为瞬时状态变化，颜色、符号和描边仍保留状态信息。
* **关卡稳定性修正**：第 26、27 关替换为同属第三章参数范围但在并行验证下稳定的
  6 色布局；重新计算并固化对应 `par`，不保留临界超时的候选关。
* **本轮边界**：未做选关、结算、存档、提示 Worker、星图或门户接入。

### 追加 · 验证

* `node --test games/orbit-sort/tests/engine.test.mjs games/orbit-sort/tests/solver.test.mjs games/orbit-sort/tests/levels.test.mjs games/orbit-sort/tests/markup.test.mjs`：24 通过，0 失败。
* `node --check` 验证 `main.mjs`、`renderer.mjs`、`audio.mjs` 通过；本地服务页面、样式及三个模块均返回 HTTP 200。
* 未触及门户、`games.json`、构建脚本或部署，按仓库规则无需运行 `npm run build`。

### 追加 · 《星轨调度大师》阶段 3：可玩 SVG 棋盘

* **新增无动画对局页面**：`games/orbit-sort/index.html`、`css/style.css`、
  `js/main.mjs` 与 `js/renderer.mjs`。默认载入第 1 关，手机以棋盘优先布局，桌面改为
  左信息栏、中央棋盘、右工具栏；不依赖框架、CDN 或远端资源。
* **新增 keyed SVG 渲染**：固定 `background / tracks / track-effects / orbs / core /
  overlay-effects` 图层。每颗星体按稳定 `orb.id` 复用 SVG 节点，状态更新只更新坐标与
  属性，不使用 `innerHTML` 整体重绘棋盘。
* **接入基础输入**：点击入口星体调入、点击中转槽选择、点击目标轨道落轨；轨道与槽位
  使用可聚焦 SVG button 语义，支持 `Enter`/`Space`、`1-8`、`[`/`]`、`Z`/`Ctrl+Z`
  和 `R`，并提供撤销、重置与 `aria-live` 状态播报。
* **新增标记测试与 favicon**：`tests/markup.test.mjs` 检查单一 `h1`、可访问工具、SVG
  图层、稳定节点及不使用整块 `innerHTML`；新增本地 `favicon.svg`。
* **本轮边界**：未做动画、音效、选关、结算、存档、提示 Worker 或门户接入。

### 追加 · 验证

* `node --test games/orbit-sort/tests/engine.test.mjs games/orbit-sort/tests/solver.test.mjs games/orbit-sort/tests/levels.test.mjs games/orbit-sort/tests/markup.test.mjs`：23 通过，0 失败。
* `node --check games/orbit-sort/js/main.mjs` 与 `node --check games/orbit-sort/js/renderer.mjs`：通过。
* 本地静态服务 `http://127.0.0.1:46811/games/orbit-sort/`：页面、模块和样式均返回 HTTP 200；本轮未触及门户、`games.json`、构建脚本或部署，无需运行 `npm run build`。

### 追加 · 《星轨调度大师》阶段 2：完整主线关卡与特殊轨道

* **主线扩展至 30 关**：`games/orbit-sort/levels.mjs` 已固化三章完整关卡。第 11-20
  关进入 5 色容量 4 与冻结轨道，第 21-30 关进入单向轨道、6 色和低空位调度；所有
  主线数据为确定布局，不在运行时随机生成。
* **关卡状态构造统一**：新增 `createLevelState(level)`，将关卡 `modifiers` 映射为
  引擎轨道状态，确保 `frozen`、`in-only`、`out-only` 不只是数据字段而是真实规则。
* **特殊机制已验证参与解法**：冻结关必须先完成绑定颜色才能在求解路径中调出冻结轨道；
  `in-only` 关实际落入该轨道，`out-only` 关实际从该轨道调出。
* **性能取舍**：淘汰了在整套验证中接近 5 秒阈值的第 29 关候选，改为稳定可验证的
  6 色低空位布局；不以“偶尔能在预算内解出”为主线关标准。
* **本轮边界**：阶段 2 完成；未创建 SVG 页面、Worker、存档、音频或门户接入。

### 追加 · 验证

* `node --test games/orbit-sort/tests/engine.test.mjs games/orbit-sort/tests/solver.test.mjs games/orbit-sort/tests/levels.test.mjs`：21 通过，0 失败；30 关 `par` 与 0-1 BFS 最优值一致，路径复放均通关。
* 未触及门户、`games.json`、构建脚本或部署，按仓库规则无需运行 `npm run build`。

### 追加 · 《星轨调度大师》阶段 2：求解器与前 10 关

* **新增 0-1 BFS 求解器**：`games/orbit-sort/solver.mjs`。调入动作成本为 1，落轨动作
  成本为 0；每个原子动作后去重，状态键包含轨道顺序、模式、完成/冻结状态、中转槽
  状态与来源轨道，当前选中槽不参与去重。
* **新增前 10 个固定主线关卡**：`games/orbit-sort/levels.mjs`。第 1-5 关为第一章
  单槽教学段，第 6-9 关使用 4 色双槽，第 10 关进入容量 4；所有布局均为确定数据，
  未使用特殊轨道，`par` 由同一版本 0-1 BFS 计算并固化。
* **新增求解器与关卡测试**：验证状态键信息完整、超时不误报无解、前 10 关参数与颜色
  计数正确、无预解、`par` 最优，并逐步复放每关路径至 `isSolved`。
* **本轮边界**：尚未扩展到 30 关和特殊机制，未创建 SVG 页面、Worker、存档或门户接入。

### 追加 · 验证

* `node --test games/orbit-sort/tests/engine.test.mjs games/orbit-sort/tests/solver.test.mjs games/orbit-sort/tests/levels.test.mjs`：20 通过，0 失败。
* 未触及门户、`games.json`、构建脚本或部署，按仓库规则无需运行 `npm run build`。

### 追加 · 移除疯狂扭螺丝（nuts）

* **下线子游戏**：从 `games.json` 移除 `nuts` 发布条目，并删除
  `games/nuts/` 的全部源码与本地探针文件。站点构建后不再生成 `/nuts/`，首页、
  JSON-LD 和 sitemap 也不再把它作为可访问游戏。
* **保留历史记录**：早期 `PROJECT_LOG.md` 的开发事实与《星轨调度大师》产品研究中的
  通用品类引用不属于运行时游戏实现，未做篡改；本轮删除的是已发布的 nuts 子游戏本体。
* **验证**：`npm run build` 退出 0；`games/nuts` 与 `dist/nuts` 不存在，构建后的
  `games.json` 和 `sitemap.xml` 均不含 nuts。

### 追加 · 《星轨调度大师》阶段 1：纯引擎

* **新增不可变规则引擎**：`games/orbit-sort/engine.mjs`。实现 `createState`、调入、
  槽位选择、合法落轨、完成锁定、冻结解除、单向轨道、卡住检测、无限撤销和重置；
  轨道数组始终按“远端到入口”保存，调入 `pop()`、落轨 `push()`。
* **锁定步数与快照语义**：仅调入增加 `moves`；每次调入保存完整调度前快照，故即使
  已完成轨道并触发解冻，也能由一次撤销准确恢复。
* **新增引擎回归测试**：`games/orbit-sort/tests/engine.test.mjs`，覆盖入口/槽位/落轨
  限制、完成与通关、冻结与单向、选槽、卡住、撤销、重置、星体 id 不变及不可变更新。
* **本轮边界**：未创建页面或 SVG 渲染，未实现求解器、关卡、存档或门户接入；未改动
  `games.json`、构建脚本和其他子游戏。

### 追加 · 验证

* `node --test games/orbit-sort/tests/engine.test.mjs`：14 通过，0 失败。
* 未触及门户、`games.json`、构建脚本或部署，按仓库规则无需运行 `npm run build`。

### 追加 · 《星轨调度大师》产品文档

* **新增可直接进入开发的 V1 PRD**：`docs/PRD-star-orbit-master-v1.md`。产品从
  Nut Sort 的“顶层取物 + 同色归组 + 空间周转”出发，改为环形放射星轨和中央双中转槽；
  中转槽是真实状态空间，不是视觉换皮。
* **锁定产品范围**：30 个求解器验证的主线关、冻结 / 单向两种特殊轨道、三星、
  三章星图、每日挑战、本机存档、SVG 棋盘、WebAudio、键盘和色觉辅助；不做广告、
  内购、账号、倒计时、WebGL 或未经验证的无限随机关。
* **锁定开发契约**：文档给出核心状态机、数组方向、计步规则、完成 / 卡住判定、
  关卡数据格式、求解器动作与剪枝边界、SVG 坐标、响应式尺寸、文件结构、测试命令、
  完整的手工 / 自动验收要求及分阶段开发顺序。
* **本轮边界**：只交付产品文档，未创建半成品游戏目录，未修改 `games.json`，未接入门户。

### 追加 · 文件改动

* `docs/PRD-star-orbit-master-v1.md`（新增）、`PROJECT_LOG.md`（本节）。

### 追加 · 验证

* 文档结构与关键契约已静态检查；本轮未触及门户、游戏列表或构建脚本，按仓库规则
  无需运行 `npm run build`。

### 完成

* **修复通关判定逻辑 Bug（nuts）**：旧代码 `capacity(st) = stack + extraRods`，
  一旦使用「加杆」，`isSolved` 会要求每根杆填满 `stack + extraRods`，导致
  同色已归拢的 `stack` 层杆判为未通关——玩家感觉是"某颜色必须在某杆"。
  改为单杆容量恒为 `stack`，「加杆」只追加一根空的暂存杆；通关=每根非空杆满
  `stack` 且单色（空杆忽略）。求解器生成期 `extraRods=0`，行为不变。
* **游戏区视觉重构：2D 正交深海军蓝舞台（取代上面的 3D 方案）**：CSS 3D 棱柱
  （perspective / preserve-3d / rotateX / translateZ / 6×side + 2×cap）已全部移除，
  改为纯 2D 正交渲染。动机是 3D 方案下"中心孔与螺杆中轴对不齐"（孔偏移、杆像贴在
  螺母背后、螺母悬浮在杆前）反复调不干净——2D 下孔圆心天然落在螺杆中轴上。要点：
  - **孔用 `mask-image` 真挖穿**：`.n-face` 用 `clip-path` 出正面六角形，再用
    `radial-gradient` 遮罩把中心孔挖空露出背后的 `.rod-shaft`，杆从孔里**连续穿出**
    而不是画一个黑洞；`.n-hole` 只画孔沿倒角（上唇深色 / 下唇浅色）。
  - **几乎正视的正交视角**：无透视收缩，所有螺杆严格平行、等宽、等距、等高
    （`rodH = availH`，垂直贯穿整个棋盘）；螺母以正面六角形为主，只靠 `--bevel`
    在顶部做亮面、右侧做窄暗面，表达约 10–15° 俯视感；底座用略扁椭圆表轻微俯视。
  - **高度反解布局**：已知可用高度时反解
    `nutW = (availH - TOP_PAD - BOT_PAD) / (NUT_ASPECT × ((cap-1)(1-STACK_OVERLAP) + 1))`，
    与宽度约束取 `min`，保证堆叠接近满屏、棋盘占可视区 70% 以上，而非固定尺寸居中。
    `NUT_MAX` 提到 100 让少杆关卡也撑得开；`gap` 按杆数分档（≤6:11 / ≤8:7 / 更多:5）。
  - **深海军蓝舞台**：`#121e42 → #0b142e → #05091a` 极弱纵向渐变，无毛玻璃、无留白。
    HUD 压成单行细条、工具栏按钮 52×34，关卡包 tabs 只在选关屏显示
    （`body[data-screen="play"] .tabs { display:none }`）。
  - 螺母改高饱和 9 色（红蓝绿紫橙黄洋红青黄绿）三段 `--c-hi/-mid/-lo`，低模塑料
    质感而非写实金属高光；杆顶叠退色渐变避免空杆头抢戏。

### 文件改动

* `games/nuts/engine.mjs`（capacity / 通关注释）、`games/nuts/game.js`
  （正交几何常量、`computeLayout` 高度反解、`renderBoard` 2D DOM、`switchScreen`
  的 `data-screen`）、`games/nuts/style.css`（整体重写：舞台 / rod-shaft 连续螺纹 /
  rod-base 椭圆盘 / `.nut` + `.n-face` + `.n-hole` / 紧凑 HUD 与工具栏 / 深蓝弹层）。

### 验证

* 关卡生成脚本：25 关全部非预解 + DFS 可解 + par>0（ALL 25 LEVELS OK）。
* 回归脚本：连用两次「加杆」后按解法走完 `isSolved` 仍为 true（旧代码为 false）。
* `npm run build` 退出 0。

### 追加 · 2048 首页封面图

* **新增 630×500 封面（`assets/covers/2048.png`）**：暖米底 + 游戏同款配色的 3D
  数字方块，中心高亮 2048 方块，四周 2/4/8/16/64/512 与滑动箭头。`games.json` 里
  2048 的 `cover` 已从空串改为该路径，首页卡片由 emoji 切换为图片。
* **路径选择**：封面放在门户 `assets/` 而非 `games/2048/`。构建把 `assets/` 原样复制到
  站点根，本地（仓库根）与线上（dist 根）都能用同一个 `/assets/covers/2048.png`；
  放进游戏目录则只能命中线上路径 `/2048/…`，本地会 404。
* 出图工具带右下角水印，裁掉底部 35px 一并去除；未做 inpainting。

### 追加 · 文件改动

* `assets/covers/2048.png`（新增）、`games.json`（2048 `cover`）、`.gitignore`
  （忽略 `vibe_images/` 出图中间产物）。

### 追加 · 验证

* `npm run build` 退出 0，`dist/assets/covers/2048.png` 已生成，`dist/games.json`
  的 cover 路径一致。
* 页面渲染效果由用户通过 `start.bat` 自行验收。

***

### 2026-09-01 · orbit-sort 规则状态修复

修复 `stuck` 状态遮蔽合法操作的问题：`stuck` 仅表示当前局面无可行动作，
不再阻止真实合法的取出、选槽与同色/空轨落入；仅 `won` 禁止继续操作。同步修正
页面状态迁移使用旧状态判断弹窗的问题，并新增过期 `stuck` 状态合法落轨回归测试。
模块缓存版本升级为 `20260901-rules-v3`，防止浏览器继续使用旧规则代码。

***

## Current status · 2026-08-30

### What shipped

* **新游戏 2048（`games/2048/`，已上线）**：依据用户提供的参考构建包重新实现。
  该参考包是 TanStack Start + React 19 + Tailwind v4 工程，含 auth / pglite /
  multiplayer 脚手架——这些与玩法无关，一概不复制；最终只保留其核心玩法与交互体验。
* **技术形态**：纯静态、零依赖、与 `games/nuts` 同构。原生 ES modules + 原生 CSS，
  系统字体栈（替代 Google Fonts 外链）、内联 SVG 图标（替代 lucide-react）、
  WebAudio 实时合成音效（零音频文件）。**页面无任何外部请求，可离线运行。**
* **模块**：`js/engine.mjs`（纯函数，可被 `node:test` 直接引入）、`storage.mjs`、
  `i18n.mjs`、`audio.mjs`、`input.mjs`、`ui.mjs`、`main.mjs`，外加 `index.html`、
  `css/style.css`、`favicon.svg`。
* **玩法与交互**：4×4；一次移动内不连锁合并；无效移动不补块、不进撤销历史；
  键盘 / WASD / 触屏滑动 / 移动端十字键；撤销一步、二次确认重开、胜利后可继续；
  统计（局数 / 通关 / 最大方块）；`doin.2048.*` 本机存档；浅深主题；中英文案；
  reduced-motion 与屏幕阅读器播报。
* **视觉**：奶油纸质风——配色、字体、阴影全部重新设计，观感贴近参考包但不复用其素材。
* **测试**：`games/2048/tests/` 四个文件、48 个 `node:test` 用例全通过。
* **已提交并部署**：`8d0f384`（nuts 与启动脚本）、`62fa64b`（2048 与构建排除），
  推送后 GitHub Actions 部署成功，`doin.win/2048/` 线上验证 200。

### Files changed

* New: `games/2048/{index.html, favicon.svg, css/style.css, js/*.mjs, tests/*.test.mjs}`
* Updated: `games.json`（2048 条目）、`scripts/build-site.mjs`（静态游戏走带过滤的
  `copyGame()`）、`.gitignore`（忽略 `.workbuddy-ai/`）、`AGENTS.md`、`README.md`
* Removed: `games/2048/2048-web/`（参考构建包，分析完成后删除，从未入库）

### Key implementation

* **引擎**：`applyMove` 返回 `{state, moved, gained, topMerge, absorbed}`。
  `absorbed` 带父块 id 与合并目标格坐标，渲染层据此让父块**滑向**合并格。
* **渲染**：keyed tile（`Map<id, node>`）复用 DOM 节点；`releaseGhosts()` 在 settle
  时清除幽灵块。**不能用整体 re-render 代替**——会掐断仍在播放的入场动画。
* **定位**：`transform: translate(calc(var(--c) * (100% + var(--gap))), ...)` 并过渡，
  替代参考包的 `@property --r/--c` 方案，兼容性更好。
* **存储**：`doin.2048.{save,best,stats,prefs}`。读取时做结构校验（丢弃非法 tile、
  同格去重、坏数字归零），`best` 取存档与独立键的 max，因此清档不丢纪录。
* **构建排除**：`copyGame()` 始终跳过 `node_modules / dist / tests / 点开头目录`，
  再叠加 `games.json` 条目里可选的 `exclude`。`tests/` 这条是必需的——2048 的测试
  就放在源码旁边，否则会被拷进产物。

### Issues hit

1. 参考包位于 `games/2048/2048-web`，而构建对无 package.json 的游戏是整目录拷贝，
   会把 937K 打进产物。先按用户选择给构建脚本加排除规则；分析完成后参考包删除，
   故 `games.json` 未留 `exclude` 条目（能力保留备用）。
2. 存储层初版「web 存储优先 + 内存兜底」在写入成功时仍留内存影子副本，导致读到
   上一条用例的脏数据。改为 `persistent` 标志：存储可用时它是唯一权威，写失败后才
   整体切到内存。
3. 随机对局不变量测试最初用 `spawn: false`，棋盘永远填不满、`isOver` 永不成立 →
   死循环。改为默认补块。
4. Node 22 下 `node --test tests/` 会把目录当模块路径报错，必须给文件名或 glob。

### Next

* 为 nuts 的求解器 / 生成器补 `node:test` 回归测试并纳入 CI。这是上一轮踩过
  「满单色杆剪枝误判」后留下的唯一待办，没有测试守护容易复发。

***

## Archive

### 2026-08-29 · 疯狂扭螺丝（`games/nuts/`）

螺丝螺母色彩排序：顶端螺母可放入任意有空位螺杆（允许混色堆叠），全部螺杆空或
单色满杆即通关。常规 20 关 / 每日挑战（日期种子）/ 极限 5 关（每通 5 关解锁 1 关）。

* **生成器**：`levelSeed = mulberry32(FNV(pack, index|dateKey))`；从已解态随机合法
  乱序，再用有界 DFS 校验（空杆对称剪枝 + 满单色杆剪枝，节点 160k / 260ms），
  解的长度即该关 PAR；失败则换种子重试，兜底降低乱序强度。
* **存档**：`doin.nuts.save.v1`，每日存档按 dateKey 失效。**星级**：≤PAR 三星、
  ≤PAR+3 二星，moves ≤ PAR 记 PERFECT ★；连胜只计首次通关。
* **start.bat**：探测 46810–46909 取第一个空闲端口后延迟开浏览器。
  ⚠️ 本地路径 `/games/<slug>/`，生产路径 `/<slug>/`，不要混用。
* 同步删除了旧的 `games/nuts/`（彩色螺母大搬家）与 `games/nuts2/`（首版）。

### 2026-08-29 · 门户初始部署

门户重建，数独 SPA 迁入 `games/sudoku/`（Vite + TanStack Router，base `/sudoku/`）；
GitHub Actions 在 main push 时构建并 force-publish 到 `gh-pages`，`CNAME = doin.win`。

### Known platform quirks

* GitHub Pages 对浏览器历史 SPA 的深链返回 404（虽会返回回退主体），已从 sitemap 排除。
* 部署工作流的 `actions/checkout@v4` / `actions/setup-node@v4` 尚未升到 Node 24
  兼容的大版本，CI 有弃用警告，非阻塞。

# AGENTS.md

> 核心准则：平台底线铁律化、游戏风格百花齐放、经典规则深度对标、桌面美学游戏化。
> 实现细节与排障记录查阅 PROJECT_LOG.md，提交历史查阅 git log。

***

## 1 · 门户平台架构与部署准则

DOIN（`doin.win`）是不断演进的**集合型独立网页小游戏门户**。平台会持续收录、开发各类新游戏，不局限于现有品类。

- **源码与构建分离**：`main` 分支纯源码，**严禁提交 `dist/` 或任何子游戏构建产物**。
- **自动部署**：`main` 推送触发 GitHub Actions（Node 22），全量构建至 `dist/` 并覆盖发布到 `gh-pages`。生产域名为 `doin.win`（Workflow 自动注入 `cname: ${{ vars.PAGES_CNAME }}`）。
- **全局通用标签集中注入（硬性红线）**：统计代码（GA4 `G-D67E3XTNSS`）、Google AdSense、站点级 meta 统一由 `scripts/build-site.mjs` 在 `npm run build` 时向 `dist/` 所有 HTML 的 `<head>` 自动注入。**严禁在首页或任何子游戏源文件中硬编码全局统计/广告标签**。
- **路径与缓存隔离**：
  - 本地开发路径为 `/games/<slug>/`，生产发布扁平为 `/<slug>/`。
  - **游戏源码内部一律使用相对路径**，绝对禁止写 `/games/<slug>/` 或 `/<slug>/`（唯一绝对路径例外是返回门户首页 `<a href="/" id="back-home">`）。
  - 本地所有 `<link>` 和 `<script>` 必须附带 `?v=dev` 占位，构建脚本会自动替换为统一的 `BUILD_ID` 进行缓存失效。

***

## 2 · 灵活高效的三种开发协作模式

DOIN 不拘泥于僵化流程，根据场景灵活采用三种协作模式：

```
【模式 A: 本地主代理端到端开发】（最常用、高效率）
 用户需求 ──► 主代理对标经典与算法 ──► 本地编写游戏与美学UI ──► 原生测试+质检 ──► 组装构建上线

【模式 B: 策划立项先行】（稳妥把控、复杂机制或重大创新首选）
 用户模糊创意 ──► 技能: doin小游戏开发策划书 ──► 产出 PRD ──► 评审拍板 ──► 主代理开发或外包

【模式 C: 外包解耦发包】（分工协作、利用外部大模型网页版）
 确定玩法 ──► 技能: doin外包小游戏开发 ──► 产出免上下文任务书 ──► 外部AI产出 games/<slug> ──► 主代理验收组装
```

### 2.1 模式 A：本地主代理直接开发（默认）
本地主代理直接在本地仓库完成新游戏开发，遵循以下闭环：
1. **调研经典**：快速对标历史经典与成熟玩法，锁定成熟算法模型与手感细节；
2. **编写代码**：在 `games/<slug>/` 内按照 Plain 规范编写语义 HTML、双栏沉浸样式与 DOM-free 纯模块；
3. **算法与测试**：编写求解器或数学验证脚本，配套四类原生单元测试（`tests/*.test.mjs`）；
4. **门户组装**：生成统一 3D 软胶封面、登记 `games.json`、注册根 `package.json` 测试脚本；
5. **门禁验证**：跑通 `node scripts/check-game.mjs <slug>` 与 `npm run build`，本地服务预览。

### 2.2 模式 B：策划立项先行（复杂项目）
当用户提出新颖玩法或需多角色权衡时，调用技能 `skill/doin小游戏开发策划书`：
- 结合已有游戏品类与封面饱和色排重，对标 3-5 款成熟标杆；
- 产出高完成度自包含 PRD（`docs/plans/<slug>-prd.md`），明确规则建模、三档数值算例与技术方案；
- 用户确认 PRD 后，无缝转入模式 A（主代理直接动工）或模式 C（外发制作）。

### 2.3 模式 C：外包任务书发包（多端协作）
当用户需要发给外部 AI（如网页版 Gemini、Grok build、Claude）开发时，调用技能 `skill/doin外包小游戏开发`：
- 产出**零仓库上下文、免外部依赖**的三轮交付外包任务书 `docs/outsource/<slug>-spec.md`；
- **发包契约红线**：外部承接方**仅在 `games/<slug>/` 目录内产出完整代码与测试**，绝对不触碰门户外层（根目录文件、部署配置等）；
- 外部 AI 交付后，本地主代理负责机器质检（`check-game.mjs`）、封面生成、门户登记与构建验证。

***

## 3 · 算法健全性与成熟规则调研铁律（防算法出错）

小游戏看似简单，但底层逻辑往往包含严密的数学模型。**严禁闭门造车、凭空脑补规则**：

1. **必须对标经典与成熟实践**：
   - 绝大多数玩法（如三消下落、俄罗斯方块、扫雷、华容道、连线、拼图、贪吃蛇、接龙等）在数十年的游戏史上均有最优范式。
   - 开发前必须借鉴成熟实现：状态机流转、操作缓冲（Input Buffering）、连击判定、撤销栈设计等成熟模式，不可自创别扭的怪异规则。
2. **数学可解性与生成器保证（严禁死局）**：
   - 凡涉及关卡生成、随机洗牌、迷宫或网格生成的游戏，**必须从数学上保证 100% 可解**：
     - *滑块/推盘*：必须跑 BFS 验证目标态可达，并实算最优步数；
     - *连线/一笔画*：必须基于欧拉路径生成，或自终点逆向打乱；
     - *洗牌/拼图*：必须使用成熟的无不动点打乱（Derangement），保证初始局面全打乱且可收敛；
     - *扫雷*：必须延后布雷（首击安全展开）并结合逻辑求解器，杜绝靠运气猜雷；
     - *消除/俄罗斯方块*：必须采用国际 7-Bag 等公平随机算法，防止连出死块暴毙。
3. **确定性测试与自动化验证**：
   - 随机逻辑必须支持注入确定性种子 PRNG（如 `mulberry32`）；
   - 在单元测试中必须编写**大步数随机游走测试（至少 1000 步）**，验证状态机在任意合法操作序列下绝不抛错、不卡死、不变式不破。
4. **物理与时序可测试性**：
   - 连续运动或物理碰撞游戏（如矿工、弹珠、弹道肉鸽），必须基于**纯函数帧步进（`stepFrame(state, dt)`）**或固定物理步长（Fixed Timestep），确保物理状态可测试、重放一致。

***

## 4 · 桌面端 UI 美学与游戏化设计标准（防排版死板）

DOIN 是集合型游戏站，每款游戏都应具备独特的审美与沉浸感。**严禁把游戏做成粗糙老套的居中网页表单**：

1. **桌面端双栏沉浸舞台（拒绝单列拉伸）**：
   - 移动端（≤768px）采用单列紧凑自适应布局；
   - 桌面端（≥900px）**必须设计舒展的双栏或宽屏沉浸游戏舞台**：
     - 主舞台（Canvas 或网格 Board）居中或占左侧核心区域，尺寸自适应视口高度（如 `max-height: calc(100dvh - 120px)`），比例协调；
     - 右侧/侧边通栏集成精致的游戏化 HUD：关卡匾额、多维计分明细、操作控制台、提示/撤回工具盘、游戏说明。
2. **定制化题材美学（赋予游戏专属灵魂）**：
   - 依据玩法题材定制专属视觉风格（如中国风鎏金与回纹、赛博霓虹微光、复古像素街机、极简原木玩具、梦幻马卡龙糖果等），杜绝清一色死白背景与原生按钮；
   - 背景使用**环境渐变光晕与细微噪点/纹理**（如 CSS 径向光斑 drift、柔和暗角），营造深邃的游戏舞台感。
3. **游戏化 HUD 与控件质感**：
   - **HUD 徽章化**：记分牌、关卡号、计时器使用胶囊/卡牌/牌匾造型，搭配微质感双层软阴影或细微边框，数值变化附带跳动动效（bump）；
   - **实体触感按键**：操作按钮具备立体微质感、悬停微浮起（`translateY(-2px)`）与点击物理下沉（`translateY(1px)`），拒绝生硬扁平灰块；
   - **仪式感结算反馈**：通关结算弹出时，必须有层次感动效（胜利用色、星级依次点亮、新高分徽章、微粒子喷发），增强成就感。
4. **动效降级与可访问性**：
   - 所有动画与过渡必须受 `@media (prefers-reduced-motion: reduce)` 约束，降级为无过渡瞬移，确保无障碍体验。

***

## 5 · 通用工程规范与八大核心红线

### 5.1 标准模块分层（Plain 静态架构，占 90%）
除极复杂 TS 游戏采用 Buildable SPA 外，首选纯静态架构：
- `index.html`：纯语义标记骨架，外链样式与脚本，必含 `<a href="/" id="back-home">` 与 `<noscript>`。
- `js/engine.mjs`：规则唯一权威，纯函数，**DOM-free**（绝对禁止碰 DOM 与 localStorage）。
- `js/game.mjs`：DOM-free 状态控制器，接收 UI 意图，调度 engine，派发事件。
- `js/ui.mjs` 或 `js/render.mjs`：唯一碰 DOM 或 Canvas 绘制的层。
- `js/score.mjs`：计分唯一口径（UI 不得自算分），上限钳制，永不显示 `得分 > 总分`。
- `js/storage.mjs`：存档唯一口径，Key 统一为 `doin.<slug>.v1`，集中 try/catch，损坏自动 normalize 退回默认，静默降级内存。
- `js/i18n.mjs`：统一读写全站共享偏好 `localStorage["doin.lang"]`，双语表严格对齐非空。
- `js/audio.mjs`：WebAudio 程序化合成音效（零外部音频文件），手势解锁，静音或不支持时静默降级。
- `js/main.mjs`：装配入口，绑定事件，协调全局。
- `tests/*.test.mjs`：原生 `node:test`，覆盖规则、存储、i18n 与标记装配契约。

### 5.2 八大不可违背不变量（Invariants）
1. **合法操作铁律**：凡满足合法性判定的操作**必须执行**，任何层不得拦截或伪装失败；无效意图返回 `action: null` 静默忽略，**严禁使用 `alert()` 报错**。
2. **DOM-Free 规则隔离**：页面或 UI 层不得自造 action、不得直接改动 engine 内部状态；终局（won/lost）操作一律 no-op。
3. **存储稳健性**：localStorage 不可用或数据损坏时静默降级，读取数据严格归一化校验，绝不白屏。
4. **全站 i18n 共享偏好**：统一使用 `localStorage["doin.lang"]`（zh/en），禁止私有语言 key。
5. **零外部网络依赖**：零 CDN、零 webfont、零外链音视频与图片，仅使用系统 sans-serif 字体栈。
6. **路径与缓存占位**：代码内一律相对路径；本地资源一律附加 `?v=dev` 占位符。
7. **体验兜底**：必须含返回首页链接、`<noscript>` 提示、动效降级支持、适配桌面大屏与 390px 移动视口。
8. **封面风格统一**：所有 `assets/covers/<slug>.webp` 严格遵循 `docs/COVER-STYLE.md` —— 3D 软胶质感 / 单一居中主体 (60-75%) / 彩色渐变背景 / 漂浮粒子 / **零文字** / 640×640 WebP。

***

## 6 · 典型玩法架构范式速查（开发新游戏参考）

面对各具特色的新游戏，可对标以下 5 种成熟范式：

| 玩法大类 | 典型对标与已有参考 | 核心引擎与算法要点 | 渲染与桌面美学建议 |
|---|---|---|---|
| **离散网格益智** | orbit-sort, klotski, sudoku | 纯状态转移矩阵；BFS/无猜求解器保证可解；撤回栈完备 | DOM Grid 或精美 Canvas；桌面端左棋盘右操作牌匾 |
| **程序化拼图解谜** | jigsaw, one-line | 确定性种子 PRNG；欧拉路径/Derangement打乱；无死局保证 | Canvas 自绘切片/纹理；环境光晕背景；碎片锁定微动效 |
| **经典对弈与纸牌** | tic-tac-toe, freecell | 完美博弈 minimax/启发式搜索；ε失误率分档；发牌种子可解性 | 拟物卡牌/棋盘；双栏沉浸布局；双击智能收牌/落子提示 |
| **反应消除与下落** | tetris-neo, zuma, 2048 | 7-Bag 算法；吸附与消除连锁状态机；平滑过渡补间 | Canvas 2D 粒子火花；赛博/霓虹光晕；连击计数 Bump 跳字 |
| **物理动作与时机** | gold-miner, Gravity-Echoes | 固定步长 `stepFrame`；碰撞检测与弹道微积分；TNT 连锁 | 宽屏游戏舞台；固定坐标系 + 容器等比缩放；实体击碎粒子 |

***

## 7 · 验证与工作纪律（Working Rules）

- **单任务原子递增**：一次只专注一个新游戏开发或独立特性的修复，修改后及时同步 `PROJECT_LOG.md`。
- **改动必测门禁**：
  - 改动子游戏 ➔ 运行 `npm run test:<slug>`（或对应测试）；
  - 接入新游戏 ➔ 运行 `node scripts/check-game.mjs <slug>` 确保 T1 零 fail；
  - 改动门户、全局构建、构建脚本 ➔ 运行 `npm run test:home` 及 `npm run build`。
- **Git 提交**：英文 Conventional Commits（`feat:` / `fix:` / `docs:` / `style:` / `refactor:` / `chore:`），Body 阐明改动原因。
